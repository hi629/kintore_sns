<div class="container">
  <p>#<%= @tag.name %></p>
</div>
<br>
<div class="squarePostList container">
    <% @posts.each do |post| %>
    <div class="squarePostItem">
        <% if !post.image.blank? %>
            <%= link_to(post, class:"squarePostLink") do %><!-- プレビューさせる -->
                <%= image_tag(post.image.to_s, class:"squarePostImage")%>
            <% end %>
        <% end %>
    </div>
    <% end %>
</div>